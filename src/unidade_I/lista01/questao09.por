programa {
    funcao inicio() {
        real valor
        real desconto

        escreva("Entre com o valor: ")
        leia(valor)

        desconto = valor * 12.0/100.0
    
        escreva("Desconto: ", desconto)
    }
}
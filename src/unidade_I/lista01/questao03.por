programa {
    funcao inicio() {
        real base, altura
        real area

        escreva("Entre com o tamanho da base: ")
        leia(base)

        escreva("Entre com o tamanho da altura: ")
        leia(altura)

        area = (base * altura) / 2
        
        escreva("Área: ", area)
    }
}